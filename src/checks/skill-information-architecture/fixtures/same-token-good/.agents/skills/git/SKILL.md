---
name: git
description: Routine Git work with repository background.
---
# Routine branch work

Switch with `git checkout <branch>` and update with `git pull --ff-only`.

# Repository background

The release branch mirrors the deployment train, while topic branches remain short lived.
