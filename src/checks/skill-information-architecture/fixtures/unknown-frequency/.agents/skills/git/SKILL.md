---
name: git
description: Git synchronization and interactive rebase workflows.
---
# Interactive rebase

Fetch, inspect the graph, create a backup, plan each commit action, resolve conflicts carefully, validate the patch range, and publish only with explicit permission.

# Branch synchronization

Run `git checkout <branch>` followed by `git pull --ff-only`.
