# Prototype sandbox

This file deliberately supersedes the root testing rule for this subtree: code under `prototypes/` is exploratory and may ship without unit tests until it is promoted out of this directory.
