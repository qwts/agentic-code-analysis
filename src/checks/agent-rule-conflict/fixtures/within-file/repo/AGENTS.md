# Project instructions

Always run the full test suite locally before every commit; a commit without a local test run is rejected.

Use conventional commit messages (`feat:`, `fix:`, `docs:`).

Never run the full test suite locally — it is far too slow; CI owns all test execution.
